server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/subash-kharel/cloud-config
          search-paths: ecom-configs
          default-label: master
      enabled: true